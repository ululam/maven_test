## Customize dependencies
machine:
  environment:
    #JAVA_TOOL_OPTIONS: -javaagent:dfbuild-agent.jar=PACKAGING_BASE_URI=file://.,PRODUCT_KEY=dfb.test-project/,BUILD_KEY=1460139750969/,MODULES_KEY=modules/,START_TIME=0,BUILD_BASE_DIR=.
    JAVA_TOOL_OPTIONS: -javaagent:/home/ubuntu/maven_test/dfbuild-agent.jar=PACKAGING_BASE_URI=file:/home/ubuntu/maven_test/,PRODUCT_KEY=dfb.maven_test/,BUILD_KEY=1460553768978/,MODULES_KEY=modules/,START_TIME=0,BUILD_BASE_DIR=/home/ubuntu/maven_test

dependencies:
  pre:
    - wget https://s3.amazonaws.com/external-ci-artifacts/agents/dfbuild-agent.jar
    - wget https://s3.amazonaws.com/external-ci-artifacts/agents/dfbuild-agent-ext.jar

#test:
  #override:
    #- java -jar dfbuild-agent.jar -exec "mvn package"
    #- mvn package

general:
  artifacts:
    - "target/my-app-1.0-SNAPSHOT.jar"
